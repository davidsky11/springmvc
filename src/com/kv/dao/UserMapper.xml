<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kv.mapper.UserMapper">
    
    <resultMap type="com.kv.model.User" id="userMap">
        <id column="id" property="id" ></id>
        <result property="username" column="username" />
        <result property="nickname" column="nickname" />
        <result property="password" column="password" />
        <result property="email" column="email" />
    </resultMap>
    
    <select id="selectUser" parameterType="User" resultType="java.util.List" resultMap="userMap">
        select * from user where user_name = #{username} and password = #{password}
    </select>
    
    <select id="selectUserByName" parameterType="java.lang.String" resultType="java.util.List" resultMap="userMap">
        select * from user where user_name = #{username}
    </select>
    
    <insert id="addUser" parameterType="User">
        insert into user values (#{userId},#{username},#{password})
    </insert>
    
</mapper>