<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">

    <!-- 配置结果映射 -->
    <resultMap type="com.kv.model.User" id="userMap">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="nickname" column="nickname" />
        <result property="password" column="password" />
        <result property="email" column="email" />
    </resultMap>
    
    <select id="selectUser" parameterType="User" resultType="java.util.List" resultMap="userMap">
        select * from t_user where username = #{username} and password = #{password}
    </select>
    
    <select id="selectUserByName" parameterType="java.lang.String" resultType="User" resultMap="userMap">
        select * from t_user where username = #{username}
    </select>
    
    <insert id="addUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into t_user
        <set>
        	<if test="username != null">
        		username = #{username},
        	</if>
        	<if test="password != null">
        		password = #{password},
        	</if>
        	<if test="nickname != null">
        		nickname = #{nickname},
        	</if>
        	<if test="email != null">
        		email = #{email}
        	</if>
        </set>
    </insert>
    
    <select id="listUser" resultType="java.util.List" resultMap="userMap">
    	select * from t_user
    </select>
    
    <update id="updateUser" parameterType="User">
        update t_user set 
        username = #{username}, 
        nickname = #{nickname},
        password = #{password},
        email = #{email}
        where id = #{id}
    </update>
    
    <delete id="deleteUser" parameterType="int">
    	delete from t_user where id=#{id}
    </delete>
    
</mapper>